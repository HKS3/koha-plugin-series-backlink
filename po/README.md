# Translation fragments

The plugin wraps its Perl strings with `Koha::I18N`. The English source and
German translation are supplied as message-catalog fragments. Merge the
appropriate file into Koha's messages catalog and rebuild the language files
with Koha's standard translator tooling.
